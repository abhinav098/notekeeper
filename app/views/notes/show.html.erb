<p>Title: <%= @note.title %></p>
<p>Body: <%= @note.body %></p>
<p>Tags: <% @note.tags.each do |tag| %><%= tag.name %><small>(<%= tag.notes.count %>),</small><% end %></p>
<p><small>Created <%= time_ago_in_words(@note.created_at) %> ago</small></p>

<%= link_to 'Edit', edit_note_path(@note) %>
<%= link_to ' Delete', note_path(@note), method: :delete %>