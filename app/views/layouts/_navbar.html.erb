<nav class="top-bar" data-topbar role="navigation">
  <ul class="title-area">
    <li class="name">
      <h1><a href="/">NoteKeeping</a></h1>
    </li>
  </ul>

  <section class="top-bar-section">
    <!-- Right Nav Section -->
    <ul class="right">
      <%if user_signed_in? %>
        <li class="has-dropdown profile">
          <%= link_to 'Notes', '#'%>
          <ul class="dropdown">
            <li> <%= link_to "New",new_note_path %></li>
            <li> <%= link_to "My Notes",notes_path %></li>
          </ul>
       </li>   
          <li class="has-dropdown profile">
            <%= link_to current_user.email.split('@').first.capitalize, '#' %>
            <ul class="dropdown">
              <li> <%= link_to "Profile",edit_user_registration_path %></li>
              <li> <%= link_to "Logout",destroy_user_session_path, method: :delete%></li>
            </ul>
         </li>
      <% end %>
    </ul>
  </section>
</nav>